<%= form_for @alternatif, :html => {:class => "form-horizontal form-label-left"} do |f| %>
  <% if alternatif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alternatif.errors.count, "error") %> prohibited this alternatif from being saved:</h2>

      <ul>
      <% alternatif.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nama_alternatif, "Nama Alternatif :", class: "control-label col-md-3 col-sm-3 col-xs-12" %>
      <div class="col-md-6 col-sm-6 col-xs-12">
        <%= f.text_field :nama_alternatif, class: "form-control select2" %>
      </div>
  </div>
  <div class="ln_solid"></div>

  <div class="form-group">
    <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
      <%= f.submit "Simpan", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
<button type="button" value="Kembali" class="btn btn-default" onclick="window.location.href='<%= @url %> '">Kembali</button>
